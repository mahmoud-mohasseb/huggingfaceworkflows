{
  "name": "hf-flow-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.3.3"
  }
}
